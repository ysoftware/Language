func printf(format: String, arg: Int, ...) #foreign;

func main(count: Int, arguments: Int8**) -> Int {

    printf("Array: ", 0);

    array : Int[9];
    array[0] = 1;
    array[3] = 2;
    array[5] = 3;

    if (array[0] != 1) { return 1; }
    if (array[3] != 2) { return 2; }
    if (array[5] != 3) { return 3; }

    printf("OK\n", 0);

    return 0;
}