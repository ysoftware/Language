func malloc(size: Int) -> Void* #foreign;
func `free`(pointer: Void*) #foreign;
func printf(format: String, arguments: Int, ...) #foreign;

struct Node<T> {
    next: Node<T>*;
    value: T;
}

func main(count: Int, arguments: Int8**) -> Int {

    intList := new Node<Int>;
    a : Int = intList.value;
    printf("hello %d", intList.value);

    return 0;
}
