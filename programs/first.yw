func printf(format: String, arguments: Int32, ...) #foreign; 
// func printf(format: String, arguments: Int8, ...) #foreign; 

TestChar     :: "Test: %c.\n";
TestNum      :: "Test: %d.\n";
Char   :: "%c";

func main(count: Int, argument: Int8**) -> Int {

    ptr := int8PointerToInt32(*argument);
    printf(TestNum, ptr);

    char := **argument;
    // printf(Char, char);

    return 0;
}
