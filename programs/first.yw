func printf(format: String, arguments: Int, ...) #foreign; 
hello := "hello %d";
func main() -> Int {
    b := true;
    a : Int = b;
    while (a < 10002) { a = a + 1; }
    printf(hello, a);
    return 0;
}