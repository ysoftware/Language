func int8PointerToInt32(pointer: Int8*) -> Int32 #foreign;
func int32AsInt8Pointer(address: Int32) -> Int8* #foreign;
func printf(format: String, arguments: Int32, ...) #foreign; 

TestChar     :: "Test: %c.\n";
TestNum      :: "Test: %d.\n";
Char   :: "%c";

func main(count: Int, argument: Int8**) -> Int {

    num := int8PointerToInt32(*argument) + 2;
    printf(TestNum, num);

    ptr := int32AsInt8Pointer(num + 1);
    //printf(TestNum, *ptr);

    char := **argument;
    return 0;
}
